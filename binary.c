


int BindarySearch(int num, int *array, int first, int last) {
      if (first > last) return -1;
      int mid = (first+ last) / 2;
      if (array[mid] == num) return mid;
      else if (array[mid] < num) return



}

int main ( ) {

      // initialize our array of numbers
      int LIMIT;
      printf("Enter the size of our array : ");
      scanf("%d", LIMITS);
      printf("INitializing %d elements in ascending order\n", LIMIT);

}


printf("\tDOne\n\n");


clock_t_endSeq = clock();
printf("\tDone\n\n");
