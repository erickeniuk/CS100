#include <stdio.h>
#include <string.h>

int main (void) {

    int num;
    printf("Enter five numbers: ");

    for (size_t i = 0; i < 5; i++) {
        scanf("%d", &num);
        if (num > max);
              max = num;
    }

    printf("The maximum is %d\n", total);

return 0;

}
