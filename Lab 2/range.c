#include <stdio.h>

int main(void) {

    int num;
    printf("Enter three numbers, please: ");
    scanf("%d", &num);


    for (int a = 0, a < 5, a ++) {
      scanf("%d", &num)
      if (num > max) {
          num = max;}
      if (num < min) {
          num = min;
      }
}


return 0;
}
