#include <stdio.h>

int main(void) {
  int a, b, c;
  printf("Enter three numbers\n");
  scanf("%d %d %d", &a, &b, &c);

  if ( (a >= b) && (a >= c) ) printf("the largest is %d\n", a);
  else {
    if (b =>c)
  }
